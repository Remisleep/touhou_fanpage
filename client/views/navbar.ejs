<div id="navbar">
    <a href="/">Home</a>
    <a href="/gameplay">Gameplay</a>
    <a href="/scoreblog/">Player Scores</a>
    <span id="navwelcome"><a href="/login">login/register</a></span>
        
</div>

<script>
    
    
    $(function(){
        var name = "<%=data.username %>";

        if(name!="") {
            ('navwelcome').html("Welcome, "+name);
        }
        
        
        /*
        var name = "<%=data.username %>";
        var greetNewcomer = "<input type='text' /> <button>login/create an account</button>";
        
        
        const sayYourName = function() {
            var vistorName = $('#navwelcome > input').val();
                $.ajax({
                    url: '/welcome',
                    type: 'POST',                    
                    data: {
                        'nombre': vistorName
                    },
                    success: (data) => {
                        //console.log(data);
                        if(data=='SUCCESS') $('#navwelcome').html("Welcome, "+vistorName+"");
                    }
                });
        }
        

        if(name==""){
            
            $('#navwelcome').html(greetNewcomer);
            $('#navwelcome > button').on('click', sayYourName);
            $('#navwelocme > input').on('keyup', (key) => {
                if(key.keyCode==13) sayYourName();
            });
            
           <a href="/login">login/create an account</a>
        }
        else {
            $('#navwelcome').html("Welcome, "+name+"");
        }

        */
    });
</script>